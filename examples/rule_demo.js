// Example JavaScript file for demonstrating rule-based transformations

var oldStyleVar = "should be const";
var anotherVar = 42;

console.log("Debug message");
console.log("Another debug message");

function example() {
    var localVar = "local variable";
    console.log(localVar);
    
    // Some other console methods
    console.error("Error message");
    console.warn("Warning message");
}

// More var declarations
var config = {
    name: "example",
    version: "1.0.0"
};

console.log(config.name);